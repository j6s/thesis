\documentclass[%
    12pt, BCOR=10mm, a4paper, bibliography=totoc, includefoot, listof=totoc,
    smallheadings
]{scrartcl}

% load packages
\usepackage[%
    a4paper, bottom=3cm, left=3cm, right=3cm, top=2.5cm
]{geometry}

\usepackage[%
    autooneside=false, headsepline
]{scrlayer-scrpage}

\usepackage[T1]{fontenc}             % font encoding
\usepackage[ngerman]{babel}          % typographical rules
\usepackage[utf8]{inputenc}          % input encoding

\usepackage[backend=biber]{biblatex} % bibliography

\usepackage[intoc]{nomencl}          % list of symbols

\usepackage[german]{fancyref}        % fancy cross referenceing
\usepackage[pdfusetitle]{hyperref}   % support for hypertext

\usepackage[scaled]{helvet}          % load helvetica font

\usepackage{booktabs}                % nice looking tables
\usepackage{graphicx}                % enhanced support for graphics

\usepackage{lipsum}                  % lorem ipsum dummy text

\usepackage{microtype}               % improve general appearance of text

\usepackage{setspace}                % space between lines

\usepackage{titlesec}                % alternative section titles

\usepackage{xcolor}                  % color extensions


% bibliography
\addbibresource{papers.bib}

% list of symbols
\renewcommand{\nomname}{Abkürzungsverzeichnis}
\renewcommand{\nomlabel}[1]{\textbf{#1}}
\makenomenclature

% set sans serif as default font family
\renewcommand{\familydefault}{\sfdefault}

% line height
\onehalfspacing

% default page style
\pagestyle{scrheadings}

% start sections on a new page
\newcommand{\sectionbreak}{\clearpage}

% adjust font size of headings
\titleformat*{\section}{\Large\bfseries}
\titleformat*{\subsection}{\large\bfseries}
\titleformat*{\subsubsection}{\normalsize\bfseries}

% save roman page number for later use
\newcounter{savepage}

% custom header and footer
\automark[subsection]{section}

\chead{}
\cfoot{}
\ofoot[\pagemark]{\pagemark}

\setkomafont{pageheadfoot}{\itshape\color{gray}}
\setkomafont{pagenumber}{\itshape\color{gray}}
